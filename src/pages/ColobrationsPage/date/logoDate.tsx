import React from "react";
import { nanoid } from 'nanoid'
export const arrLogoDate: Array<{ name: string, id: string }> = [
    { name: "logo", id: nanoid() },
    { name: "logo", id: nanoid() },
    { name: "logo", id: nanoid() },
    { name: "logo", id: nanoid() }]; 